version: 2

models:
  - name: tokenprice_stone
    description: "stONE price in USD"
    columns:
      - name: timestamp
        description: daily timestamp
        tests:
          - not_null
          - unique

      - name: price
        description: price in USD.
        tests:
          - not_null
