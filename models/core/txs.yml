
version: 2

models:
  - name: txs
    description: |-
      Harmony Transactions

    columns:
      - name: tx_id
        description: The primary key for this table
        tests:
          - unique
          - not_null
